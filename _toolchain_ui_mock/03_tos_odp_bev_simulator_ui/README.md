# TOS/ODP BEV Simulator Mock

Open `index.html` locally.

This mock demonstrates a public BEV scenario replay concept for ego path, object tracks, candidate regions, and false-target review. It uses fake geometry, fake tracks, and no production thresholds.
