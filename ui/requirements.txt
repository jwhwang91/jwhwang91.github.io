# JobOps local UI — a viewer/orchestrator over the pipeline CLI. Install alongside the
# repo's top-level requirements.txt (which provides the pipeline itself).
fastapi>=0.110
uvicorn>=0.27
httpx>=0.27        # required by starlette's TestClient (backend contract tests)
