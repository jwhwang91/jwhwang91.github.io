---
description: Draft a public ATS_LEDGER.md edit from the anonymized lessons draft
---

Evolve the public ATS ledger from accumulated lessons.

Steps:
1. Read `Format/prompts/evolve_ledger.md` (the contract), `Applications/_ledger_draft.md`
   (already anonymization-linted), and the current `Context/variants/ATS_LEDGER.md`.
   If `_ledger_draft.md` is missing, run `python main.py lessons compile` first.
2. Propose an addition/edit to `Context/variants/ATS_LEDGER.md` capturing only anonymized,
   cross-application PATTERNS — never a company name, req URL, or JD-verbatim line, and
   never a change to the claim registry / taxonomy / backbone.
3. STOP for the owner to review and commit the ledger edit by hand.
