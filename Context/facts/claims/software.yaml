# Claim registry — Gate G0 CONFIRMED (owner-reviewed 2026-07). Claims with
# status: confirmed are citable by the resume pipeline; retired ones are not.
# Every source_quote is a verbatim substring of its cited file (portfolio/facts.py).
# Validated by Format/schemas/claim.schema.json.

claims:
- id: sw-deckflip-pipeline
  status: confirmed
  anchor: deckflip
  statement: 'Designed and shipped a full-stack web app (DeckFlip) that turns a prompt into editable slide decks via a multi-pass LLM pipeline: plan, foundation, parallel per-slide generation, deterministic assembly, and lint-gated QA repair, with AI edits constrained to a validated JSON patch/action vocabulary.'
  ownership: independent
  deployment: personal
  source_quote:
    file: Context/software_projects.yaml
    quote: A multi-pass LLM pipeline (plan -> foundation -> parallel per-slide generation -> deterministic assembly -> lint-gated QA repair) produces decks
  evidence:
  - type: repo
    ref: https://github.com/jwhwang91/DeckFlip
    verifiable_by: public
  - type: live
    ref: https://deckflip.net
    verifiable_by: public
  terms:
  - llm-pipeline
  - multi-pass-generation
  - ai-presentation-generation
  - full-stack-web
  - typescript-react
  - validation-gate
  confidence: high
  notes: 'AUTHORSHIP FRAMED 2026-09. Statement previously opened "Self-built", which
    implied hand-authored TypeScript; the source header was de-asserted the same way
    ("self-built, public products" -> "public products"). He designed, directed and
    shipped the product — the repo really is TypeScript/React — so the technology
    names stay as a description of what the repo contains, but the verb no longer
    claims he hand-wrote the code.'
- id: sw-deckflip-editor
  status: confirmed
  anchor: deckflip
  statement: Designed and shipped a sandboxed in-iframe DOM editor for DeckFlip that lets users restyle generated decks live while preserving each deck's own CSS/JS/3D animations.
  ownership: independent
  deployment: personal
  source_quote:
    file: Context/software_projects.yaml
    quote: a sandboxed in-iframe DOM editor that preserves each deck's own CSS/JS/3D animations
  evidence:
  - type: repo
    ref: https://github.com/jwhwang91/DeckFlip
    verifiable_by: public
  - type: live
    ref: https://deckflip.net
    verifiable_by: public
  terms:
  - in-browser-editor
  - iframe-sandbox
  - dom-manipulation
  - live-styling
  - typescript-react
  confidence: high
  notes: 'SOURCE DE-QUANTIFIED 2026-09; metric retired. The owner removed "~3,000-line"
    from Context/software_projects.yaml because the line count is not verifiable by a
    reviewer, so sw-deckflip-editor-loc lost its source and was deleted rather than
    re-sourced. The quote was re-synced to the surviving sentence and the verb changed
    from "Built" to "Designed and shipped": the editor exists and is his product, but
    the TypeScript was LLM-assisted, so the claim describes the artifact, not
    hand-authorship.'
- id: sw-decisioncanvas-engine
  status: confirmed
  anchor: decisioncanvas
  statement: Designed and shipped a working prototype (DecisionCanvas Studio, v0.3.0) with an async execution engine that streams graph state over Server-Sent Events, a compiler that detects control cycles via Kahn topological sort, a multi-provider LLM router, and an AI-independent safety subsystem (gate chain + regime classifier) evaluated after the model decision.
  ownership: independent
  deployment: prototype
  source_quote:
    file: Context/software_projects.yaml
    quote: Working prototype (v0.3.0). Async execution engine, a pluggable LLM router across OpenAI / Gemini / Anthropic / Ollama with automatic offline mock fallback, an AI-independent safety subsystem (gate chain + regime classifier) evaluated after the model decision
  evidence:
  - type: repo
    ref: https://github.com/jwhwang91/decisioncanvas-studio
    verifiable_by: public
  terms:
  - multi-agent-workflow
  - topological-sort
  - async-execution-engine
  - safety-gates
  - fastapi
  - server-sent-events
  confidence: high
  notes: Source frames this explicitly as a 'Working prototype (v0.3.0)', so deployment is prototype (not production). Kahn topological sort is described in the objective line; the async engine and safety gates are quoted from the impact line. SOURCE DE-QUANTIFIED 2026-09; metric retired - the owner removed '~832-line' (and the '~20 FastAPI endpoints' / '80 pytest tests' counts) from Context/software_projects.yaml as unverifiable, so sw-decisioncanvas-engine-loc lost its source and was deleted rather than re-sourced. 'Self-built' became 'Designed and shipped' - the prototype is his, but the Python/TypeScript was LLM-assisted, so the claim describes the artifact, not hand-authorship.
- id: sw-voiceprint
  status: confirmed
  anchor: voiceprint
  statement: Designed and shipped a Python writing assistant (Voiceprint) that learns a writer's voice and photo-layout style from past posts, with resilient Playwright browser automation of a rich-text editor, an ffmpeg media pipeline, and a safety-first design that always stops at 'save as draft' for human approval.
  ownership: independent
  deployment: personal
  source_quote:
    file: Context/software_projects.yaml
    quote: Python assistant that learns a writer's voice and photo-layout style from past posts, then drafts new ones
  evidence:
  - type: repo
    ref: https://github.com/jwhwang91/voiceprint
    verifiable_by: public
  metrics:
  - id: sw-voiceprint-playwright-loc
    numbers:
    - 1000
    value: 1,000+ lines of resilient Playwright automation
    method: approximate line count of the automation layer
    as_of: <OWNER TO DATE>
    allowed_renderings:
    - 1,000+ lines of Playwright automation
  terms:
  - python
  - playwright-automation
  - browser-automation
  - web-scraping
  - llm-orchestration
  - ffmpeg-media-pipeline
  confidence: high
  notes: 'SOURCE DE-QUANTIFIED 2026-09. The owner removed "~3,600-line" from
    Context/software_projects.yaml as unverifiable, so sw-voiceprint-total-loc lost its
    source and was deleted. sw-voiceprint-playwright-loc STAYS: "1,000+ lines of
    resilient Playwright automation" is still verbatim in the impact line, and a metric
    is retired only when its source goes, never for symmetry. "Self-built" became
    "Designed and shipped" — the tool is his, the Python was LLM-assisted, so the claim
    describes the artifact rather than hand-authorship. The `python` term stays: unlike
    `cpp` (removed from tc-xcp-bypass and add-tcu-embedded-c in the same pass), Python
    proficiency has not been reviewed by the owner, and silently dropping it would break
    the "Python" skills chips in Context/resume.yaml and both variants. Whether Python
    should be term_capped like the C/C++ claims is an OPEN G0 QUESTION.'
- id: sw-pinterest-oauth
  status: confirmed
  anchor: pinterest-exporter
  statement: Designed and shipped a cross-platform Python/Qt desktop app (Pinterest Reference Exporter) with an OAuth 2.0 authorization-code flow on the Python standard library and no third-party OAuth dependency (loopback callback server, CSRF state validation, silent refresh-token reconnect), threaded network/IO, and native macOS/Windows packaging.
  ownership: independent
  deployment: personal
  source_quote:
    file: Context/software_projects.yaml
    quote: OAuth 2.0 authorization-code flow on the Python standard library with no third-party OAuth dependency (loopback callback server, CSRF state validation, silent refresh-token reconnect)
  evidence:
  - type: repo
    ref: https://github.com/jwhwang91/pinterest_reference_exporter
    verifiable_by: public
  terms:
  - oauth2-authorization-code
  - desktop-app
  - pyside6-qt
  - multithreading
  - cross-platform-packaging
  - rest-api
  confidence: high
  notes: 'SOURCE DE-QUANTIFIED 2026-09; metric retired. The owner removed "17-test"
    from Context/software_projects.yaml (the suite exists, the count is not something a
    reviewer checks), so sw-pinterest-test-count lost its source and was deleted rather
    than re-sourced. "built from scratch" also went: the flow really does run on the
    standard library with no third-party OAuth dependency — which is what the re-synced
    quote now says — but the Python was LLM-assisted, so the statement claims the design
    and the shipped app, not hand-authorship of the code.'
- id: sw-pathpilot-app
  status: confirmed
  anchor: pathpilot
  statement: Designed and shipped a feature-complete native macOS file manager (PathPilot) with a keyboard-first, path-first workflow — editable address bar, dense details table, and a run-any-action command palette — now in preparation for Mac App Store release (listing finalized, not yet live).
  ownership: independent
  deployment: prototype
  source_quote:
    file: Context/software_projects.yaml
    quote: A feature-complete native macOS file manager built around a fast, keyboard-first, path-first workflow — editable address bar, dense details table, and a run-any-action command palette — now in preparation for Mac App Store release.
  evidence:
  - type: live
    ref: https://jwhwang91.github.io/pathpilot-site/
    verifiable_by: public
  - type: repo_private
    ref: PathPilot native macOS app source — private repo, ~70 Swift files (owner-only access)
    verifiable_by: self
  metrics:
  - id: sw-pathpilot-swift-files
    numbers:
    - 70
    value: ~70 Swift files
    method: file count, owner-verified in the private repo (per source comment)
    as_of: <OWNER TO DATE>
    allowed_renderings:
    - ~70 Swift files
  terms:
  - macos-app
  - swiftui
  - appkit
  - keyboard-first-ui
  - storekit
  - app-sandbox
  forbidden_phrases:
  - published (App Store|Mac App Store) app
  - available on the App Store
  - github\.com/jwhwang91/pathpilot
  confidence: high
  notes: 'Per source comment: App Store listing is finalized but NOT yet live, and the app source is in a PRIVATE repo (~70 Swift files, owner-verified). Deployment MUST NOT be production and MUST NOT claim a published/available App Store app. Public rendering must cite the live product site (pathpilot-site), never the private repo URL. G0: owner to confirm current App Store status and any updated file/line counts. AUTHORSHIP FRAMED 2026-09: "Self-built" became "Designed and shipped" to match the other software claims — the app is his product but the Swift was LLM-assisted, so the claim describes the artifact, not hand-authorship. source_quote was unaffected by the 2026-09 de-quantification of software_projects.yaml and still matches verbatim; sw-pathpilot-swift-files survives because the ~70-file count is still stated in the source comment and is owner-verified.'
