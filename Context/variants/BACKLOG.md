# Backlog — learn-before-apply

`scope: backbone-gap` lessons (from `python main.py lessons compile`) append here:
skills a JD demanded that the registry could not support, worth building a small,
real project for before the next application in that direction.

Nothing here is auto-promoted into the resume or the claim registry — these are
prompts for the owner to actually go build/learn something, then bootstrap a new
confirmed claim.
